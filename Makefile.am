
SUBDIRS = lib src

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = libVES.pc

# `foreign` disables automake's GNU-mode auto-install of a GPL COPYING; we are
# Apache-2.0. Ship our license/readme files in the release tarball explicitly.
EXTRA_DIST = LICENSE NOTICE README.md SECURITY.md

